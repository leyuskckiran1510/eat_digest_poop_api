[project]
name = "eat-digest-poop-api"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = ["flask", "flask-cors", "requests", "flask-restx"]
